// import { read } from "fs";

class NewHoldingUI {

    private _readline : any;

    constructor(readline : any) {
        this._readline = readline;
    }

    enterName() : string {
        // Handles prompts for getting a name from a user. 
        // Returns entered name if valid

        return "";
    }
}